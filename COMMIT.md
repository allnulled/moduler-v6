bug: en el devbin el comando filecom estaba usando $moduler.import con variables del runtime y no dejaba usar el dist como entry + devbin loop ahora usa el dev/settings.js#loop.extensions para el refrescador + mejor reporte de ignoreds en el refrescador + normalizeObject permite que default sea un objeto de normalización para normalizar estructuras anidadas test en 408 + nuevo $compiler.inject.plain que sirve para copiar y pegar texto plano de un fichero al código + en starter se está estabilizando la entrada de Std todavía + types todavia no es una api +typeslang y los *.tyla tienen parser en pegjs y estamos con el validador + plantillas de trace in out error para $compiler.inject.template ya funcionando en algunas + algunas apis menos estables que otras como Urler (el basepather, nombre provisional) ValidationResult TypesParser TypesValidator en proceso Introspector PropertiesMerger Error.extensions triggerMethodIfExists Ansi Environmenter Asserter Checker que es inútil se desiste la anterior porque da muchas vueltas y en apis bajas no interesa ni pensar en la opción y a solas no tiene sentido porque son expresiones booleanas de js + dependencia externalizada de Std con peggyjs para types + mejorada la entrada de definiciones del Std