se permiten rutas locales con compiler.inject.modules + moduler corrige rutas (veremos, esto) automáticamente de entry.js en src (para permitir rutas locales) a dist.js en dist porque en produccion no habria ni src ni entry.js, vaya. Entonces, no era un bug, era un mal intento de uso, pero bueno, ahora inject.modules sale reforzado, y se entiende mejor, y por no ser superestricto porque ya hay muchas reglas, _getDistRootpathFromSrc, en _importFile (para parchear errores tontos, pero puede dificultarse el traceo, no es una feture estable esta parte en _importFile) pero en reserveFile y releaseFile se usa bien, y sucede porque en _wrapAsModuleInjection sí cambiamos los paths de src a dist automáticamente, y el distribution file generado lo refleja con el cambio, para que imite completamente al path del dist + se inyecta un $moduler localizado en los módulos de compiler.inject.modules ahora, apuntando a la versión dist si es un src. Entonces ya se shipean todas estas features de golpe, y inject.modules se va aclarando.