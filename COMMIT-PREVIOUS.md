bastantes. refrescador con nuevas opciones o renombradas ignoreFiles listenFiles. muchas, el comando de devbin bundle node module, el output de compiler.inject.module mejorado que era un percal, el compiler.inject.modules nuevo para poder hacer bundleos inline o hasta binarios html para el browser, en proceso está la feature de class splitability basado en esbabel, la clase reactor, y las apis de websockets, pero se aplazan por terminar splitability que era el motivador y ya no por caida de workaround, ah, y está fallando la compilación de md que hace una cosa rara ahora mismo en el 407.